// Fonction utilitaire pour générer un identifiant unique
const generateUniqueId = () => {
  // Logique de génération d'identifiant unique (utilisez UUID, par exemple)
  // Retourne l'identifiant généré
  return 'generated-unique-id';
};

// Fonction utilitaire pour formater la date au format souhaité
const formatDate = (date) => {
  // Logique de formatage de la date (utilisez Moment.js, par exemple)
  // Retourne la date formatée
  return 'formatted-date';
};

// Ajoutez d'autres fonctions utilitaires selon vos besoins

module.exports = {
  generateUniqueId,
  formatDate,
  // Ajoutez d'autres fonctions utilitaires ici
};
